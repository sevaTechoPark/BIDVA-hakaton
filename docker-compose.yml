services:
  server:
    build:
      context: ./backend/
    ports:
      - 5151:5151
  app:
    build:
      context: ./web/
      dockerfile: Dockerfile
    network_mode: "host"
    environment:
      - NODE_ENV=development
    depends_on:
      - server
